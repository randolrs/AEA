<div class="home-question-container">

	<%= form_for(@question, :html => { :class => "home-question-form"}) do |f| %>

        <% if @question.errors.any? %>

        	<div id="error_explanation">

	            <h2><%= pluralize(@question.errors.count, "error") %> prohibited this article from being saved:</h2>

	            <ul>

		            <% @question.errors.full_messages.each do |message| %>
		              
		              <li><%= message %></li>

		            <% end %>

	            </ul>

	        </div>

        <% end %>

        <%# link_to_add_fields "+ Add Forecast", f, :earnings_predictions %><%= f.text_area :body, placeholder: "Ask a Question", :rows => "2", autofocus: false, class: "question-text" %>

        <%= f.hidden_field :user_id, as: :integer, :label => false, :placeholder => current_user.id %>

        <div class="question-ask-submit-container">

        	<%= link_to_add_fields "+ Add Answers", f, :answers %>
        	<%= f.submit "Ask", class: "question-ask-submit default-off", id: "question-ask-submit" %>

        </div>

        <div class="question-audience-filter">

        	<h2>Question Text Here</h2>

        </div>

	<% end %>

</div>
